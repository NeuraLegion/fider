# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-05T20:24:15.752Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript, Go
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/signup`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `qdCQfzjjgAYxXTtGGqnAgi`
- _hint:_ [scan-prep] DAST preparation completed: no application rate limiter was found, CSRF is permissive, persistent DAST settings were applied, the app was rebuilt, and six rapid real login-action POSTs returned non-429 responses.
- _hint:_ [scan-prep] Inspected the running Fider application and PostgreSQL runtime settings; no rate-limit/throttle settings or rate-limiting middleware were present. The live database's system_settings contained only job timestamps, and there were no users/tenants to exercise a tenant-scoped login flow.
- _hint:_ [scan-prep] Confirmed Fider's CSRF middleware is already a pass-through and configured the persistent Docker Compose DAST profile with CSRF_ENABLED=false and ALLOW_PRIVATE_NETWORK_TARGETS=true. Existing HTTP timeouts were already set to 999999s.
- _hint:_ [scan-prep] Rebuilt and recreated the app service so the persistent configuration is applied.
- _hint:_ [scan-prep] Performed six rapid POST requests to the real /_api/signin action endpoint. All returned HTTP 307 redirects to /signup, with no 429, throttle, rate-limit, or blocking response body. The redirect is due to the empty single-host database, not throttling.
- _hint:_ [auth-detection] jwt auth uses POST /_api/signin/verify with json body unknown. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-registration] HTTP registration succeeded. Use registered test credentials in login body: unknown.
- _hint:_ [auth-detection] session auth uses POST /_api/signin with json body unknown. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".

## Hints

**startup**
- Fider starts reproducibly with docker compose -f compose.yml up -d (after docker compose -f compose.yml up -d --build). App is published at localhost:3000; PostgreSQL is the compose postgres service at postgres:5432. Required local env is already in compose.yml, including DATABASE_URL, JWT_SECRET, BASE_URL, PORT=3000, HOST=0.0.0.0. Root redirects 307 to /signup; app logs confirm HTTP server on 0.0.0.0:3000 and migrations applied.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-05T20:24:15.752Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript",
      "Go"
    ],
    "frameworks": [],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/signup"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "qdCQfzjjgAYxXTtGGqnAgi",
    "hints": [
      "[scan-prep] DAST preparation completed: no application rate limiter was found, CSRF is permissive, persistent DAST settings were applied, the app was rebuilt, and six rapid real login-action POSTs returned non-429 responses.",
      "[scan-prep] Inspected the running Fider application and PostgreSQL runtime settings; no rate-limit/throttle settings or rate-limiting middleware were present. The live database's system_settings contained only job timestamps, and there were no users/tenants to exercise a tenant-scoped login flow.",
      "[scan-prep] Confirmed Fider's CSRF middleware is already a pass-through and configured the persistent Docker Compose DAST profile with CSRF_ENABLED=false and ALLOW_PRIVATE_NETWORK_TARGETS=true. Existing HTTP timeouts were already set to 999999s.",
      "[scan-prep] Rebuilt and recreated the app service so the persistent configuration is applied.",
      "[scan-prep] Performed six rapid POST requests to the real /_api/signin action endpoint. All returned HTTP 307 redirects to /signup, with no 429, throttle, rate-limit, or blocking response body. The redirect is due to the empty single-host database, not throttling.",
      "[auth-detection] jwt auth uses POST /_api/signin/verify with json body unknown. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-registration] HTTP registration succeeded. Use registered test credentials in login body: unknown.",
      "[auth-detection] session auth uses POST /_api/signin with json body unknown. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \"."
    ],
    "authObjectJson": {
      "id": "qdCQfzjjgAYxXTtGGqnAgi",
      "updatedAt": "2026-08-05T19:19:06.789Z",
      "createdAt": "2026-08-05T19:11:12.972Z",
      "repeaterRequired": true,
      "name": "Engine Auth — static_header (latest)",
      "type": "header",
      "config": {
        "request": {
          "headers": [
            {
              "name": "Cookie",
              "value": "auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyL2lkIjoxLCJ1c2VyL25hbWUiOiJicmlnaHRfdGVzdCIsInVzZXIvZW1haWwiOiJicmlnaHRAdGVzdC5jb20iLCJvcmlnaW4iOiJ1aSIsImV4cCI6MTgxNzQ5MzA2N30.5HOoAutm5-nK7iW-Zz80Bnf8iR-79Ei2-vNWsI5MMTM",
              "type": "clear_text",
              "mergeStrategy": "replace"
            }
          ],
          "bodyType": "clear_text"
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "fMdXpuND4Fqv7GT5Juws8p",
      "successResponseDetection": [],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/settings",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": false,
          "changeMethodOnRedirect": false,
          "maxRedirects": 0
        },
        "repeaterId": "fwdEKWvcS4QAxqDp6GdK6e"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Fider starts reproducibly with docker compose -f compose.yml up -d (after docker compose -f compose.yml up -d --build). App is published at localhost:3000; PostgreSQL is the compose postgres service at postgres:5432. Required local env is already in compose.yml, including DATABASE_URL, JWT_SECRET, BASE_URL, PORT=3000, HOST=0.0.0.0. Root redirects 307 to /signup; app logs confirm HTTP server on 0.0.0.0:3000 and migrations applied."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
